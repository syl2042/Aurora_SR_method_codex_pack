# Completion

Before claiming completion, verify the approved objective and each still-open requirement. Report changed files, final checks, unverified runtime or E2E layers and remaining human acceptance.

Use `verified`, `user_testing` or `blocked` according to final evidence. Intermediate failures do not create a closure gate or rollback event.
