# Realign SR state after an upgrade

Start read-only. Read `AGENTS.md`, `SR_BOOTSTRAP.md`, `CURRENT_STATE`, registries, and the `selected` result from `find_next_session_prompt.py`; if it is `ambiguous`, ask for the path and use `--prompt`.

Read active `task_state.yaml` or necessary legacy contracts. Inventory open requirements, including `validated_requests`, with implementation, evidence, and acceptance. Propose the smallest realignment and `repair`, `user_testing`, or `blocked` states.

Wait for exact `je valide` approval before mutation. Then change only named memory and registries, never application code. Update `CURRENT_STATE.md` only from evidence. Do not select a resume by date, mass-convert history, or create a micro-lot for an existing requirement.
