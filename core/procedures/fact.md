# Facts

Use the source that directly decides the claim. Distinguish observed source, generated artifact, active runtime and user-visible behavior. If evidence is absent or stale, say so instead of inferring a green state.
