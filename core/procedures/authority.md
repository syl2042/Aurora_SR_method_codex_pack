# Scope Gate

Before a non-trivial mutation:

- state the verifiable objective, assumptions, smallest sufficient approach and final verification;
- confirm the allowed project, environment and paths;
- preserve local changes, secrets, data and shared resources;
- stop only when a required decision, authority or safe recovery path is missing.

An explicit validation covers the bounded pass. Do not request another approval unless scope, risk or external effect changes.
