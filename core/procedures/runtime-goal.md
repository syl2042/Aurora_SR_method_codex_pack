# Runtime goal

When an external goal runner is explicitly used, generate one bounded goal from the validated pass: objective, allowed scope, stop conditions and final verification. Do not embed the full method or silently chain another pass.
