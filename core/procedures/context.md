# Context pressure

Track real context occupancy separately from uncached input, cached input, output and tool results. Cached tokens are not removed from context pressure.

Prefer targeted reads, compact tool results and stable references. Create a handoff only when continuity would otherwise be lost; do not create one on every fixed lot count or user-turn count.
